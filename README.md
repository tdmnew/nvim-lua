# My Neovim Bindings

Constant WIP (Usually updated when I find a plugin I like).

## How to Install

- Clone this repo to `~/.config/nvim` (Unix) or `~/AppData/Local/nvim` (Windows)
- Run `:Lazy` and install all the plugins
